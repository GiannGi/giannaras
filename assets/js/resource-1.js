$(function(){    var myStack = [nav.current]; // Ξεκινάμε με την πρώτη σελίδα στη λίστα    var isGoingBack = false;    // 1. Καταγραφή κάθε αλλαγής σελίδας στη λίστα (Stack)    $(window).on('pagechange', function(e, data) {        if (isGoingBack) return; // Αν κάνουμε Undo, μην καταγράφεις        var currentPage = data.index + 1;        if (myStack[myStack.length - 1] !== currentPage) {            myStack.push(currentPage);        }    });    // 2. Εκτέλεση Undo (Πολλά βήματα πίσω)    $(document).on('click', '[name="back_btn"]', function(e) {        e.preventDefault();        if (myStack.length > 1) {            isGoingBack = true;            myStack.pop(); // Αφαιρούμε την τωρινή σελίδα από τη λίστα            var prevPage = myStack[myStack.length - 1]; // Παίρνουμε την προηγούμενη                        nav.to(prevPage); // Εντολή στον Slider                        // Ξεκλείδωμα μετά το τέλος του εφέ (600ms)            setTimeout(function() { isGoingBack = false; }, 600);        }        return false;    });});