$(function(){    var navStack = [1]; // Ξεκινάμε με την πρώτη σελίδα    var isGoingBack = false;    // Καταγραφή διαδρομής (Listener)    $(window).on('pagechange', function(e, data) {        if (isGoingBack) return;        var page = data.index + 1;        if (navStack[navStack.length - 1] !== page) {            navStack.push(page);        }    });    // Επιβολή Undo (Executor)    $(document).on('click', '[name="back_btn"]', function(e) {        e.preventDefault();        if (navStack.length > 1) {            isGoingBack = true;            navStack.pop(); // Αφαιρεί την τωρινή σελίδα            var target = navStack[navStack.length - 1]; // Βρίσκει την προηγούμενη                        nav.to(target); // Εντολή στον κινητήρα του In5                        // Ξεκλείδωμα μετά το animation            setTimeout(function() { isGoingBack = false; }, 600);        }        return false;    });});